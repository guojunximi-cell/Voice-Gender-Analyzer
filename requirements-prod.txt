# Runtime dependencies for Railway.app deployment
# Python 3.11
# Versions mirror the local working environment to avoid conflicts.
# Key constraint: pyannote.core 6.x requires numpy>=2.0,
# so tensorflow must be >=2.18 (first release supporting numpy 2.x).

# ── Web framework ──────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.0
gunicorn==23.0.0
python-multipart==0.0.20

# ── AI / signal processing ─────────────────────────────────────
# tensorflow-cpu: CPU-only wheel, no CUDA driver needed on Railway.
# 2.18+ is the first series to support numpy 2.x.
tensorflow-cpu>=2.18.0
numpy>=2.0,<2.2
scipy>=1.14.1
scikit-image>=0.24.0
soundfile==0.13.1
audioread==3.0.1
soxr==1.0.0
librosa==0.11.0


# ── Data / visualisation (inaSpeechSegmenter deps) ─────────────
pandas>=2.2.3
matplotlib>=3.9.4
Pyro4==4.82
pytextgrid==0.1.4
pyannote.core==6.0.1
pyannote.algorithms==0.8
pyannote.parser==0.8
decorator==5.2.1
sortedcontainers==2.4.0
